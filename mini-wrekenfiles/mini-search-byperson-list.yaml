VERSION: 2.0.2
METHOD:
  ID: search.byperson.list
  SUMMARY: Search Episodes by Person
  CANONICAL_ID: search.byperson.list
  DESC: |
    This call returns all of the episodes where the specified person is mentioned.

    It searches the following fields:

      - Person tags
      - Episode title
      - Episode description
      - Feed owner
      - Feed author

    Examples:

      - https://api.podcastindex.org/api/1.0/search/byperson?q=adam%20curry&pretty
      - https://api.podcastindex.org/api/1.0/search/byperson?q=Martin+Mouritzen&pretty
      - https://api.podcastindex.org/api/1.0/search/byperson?q=Klaus+Schwab&pretty
EXECUTION:
  KIND: http
  MODE: sync
  EXECUTION_LEVEL: standalone
HTTP:
  METHOD: GET
  ENDPOINT: /search/byperson
  CONTENT_TYPE: application/json
  ACCEPT: application/json
  HEADERS:
    X-Auth-Key: x-auth-key
    User-Agent: user-agent
    X-Auth-Date: x-auth-date
    Authorization: authorization
INPUTS:
  - name: q
    TYPE: STRING
    REQUIRED: true
    LOCATION: query
  - name: max
    TYPE: INT
    REQUIRED: false
    LOCATION: query
  - name: fulltext
    TYPE: BOOL
    REQUIRED: false
    LOCATION: query
  - name: pretty
    TYPE: BOOL
    REQUIRED: false
    LOCATION: query
RETURNS:
  TYPE: STRUCT(search.byperson.listResponse200)
ERRORS:
  - TYPE: STRUCT(Error400)
    WHEN: Invalid request (HTTP 400)
  - TYPE: STRUCT(Error401)
    WHEN: Not authenticated (HTTP 401)
STRUCTS:
  search.byperson.listResponse200:
    - name: status
      type: STRING
      REQUIRED: false
    - name: items
      type: ANY
      REQUIRED: false
    - name: count
      type: INT
      REQUIRED: false
    - name: query
      type: STRING
      REQUIRED: false
    - name: description
      type: STRING
      REQUIRED: false
